import sys

n = int(sys.stdin.readline())
h = [n]

for i in range(n):
    sum += 

def lowerBound(self,)

def getheight(self, node) -> int:
    lb = lowerBound(list, 0, size, node):
    if (lb > 0):
        h[list.get(lb-1)]
    else:
        0
    return 
